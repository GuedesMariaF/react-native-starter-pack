# URL base da API consumida pelo app (precisa do prefixo EXPO_PUBLIC_ para ficar disponível no client)
EXPO_PUBLIC_API_BASE_URL=

# URL do swagger/OpenAPI usada pelo orval para gerar o client da API (fallback: ./swagger.json)
VITE_API_SWAGGER_URL=
